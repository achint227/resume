/**
 * Constants barrel export
 * Provides a single entry point for all application constants
 */

// Default values for resume sections and templates
export {
  DEFAULT_SECTION_ORDER,
  ORDER_MAP,
  FALLBACK_TEMPLATES
} from './defaults';

// Configuration constants with environment variable support
export {
  API_BASE_URL,
  CONNECTION_TIMEOUT,
  REQUEST_TIMEOUT
} from './config';
